# The EXPO_PUBLIC_ prefix is required — the Expo bundler only inlines env vars
# carrying it. A bare SENTRY_DSN is never substituted into the release bundle.
EXPO_PUBLIC_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
