# G4 — Produce signed, installable builds for both stores

> ⚠️ **CORRECTIONS NOTICE — read before trusting this file** *(added 2026-07-29; every item below was re-verified by command against `main` @ `809cf37`)*
>
> This prompt is otherwise preserved verbatim as written on 2026-07-28. Its ground-truth claims were snapshots of one working copy, and the ones listed here are now **wrong or stale**. The running corrections log is [`GOAL_PROMPT_NOTES.md`](../GOAL_PROMPT_NOTES.md) at the repository root. **Measured repo state overrides this document wherever they disagree.**
>
> **Status: ✅ Completed 2026-07-28** — merged at `2378cee` (PR #12); the full evidence table is in the notes' "Goal 4 — Buildability" section. **Do not re-execute.**
>
> **Known-wrong or stale claims in this prompt:**
> - **The cleartext premise is backwards.** `android.usesCleartextTraffic` in `app.json` was *inert* — not in the Expo SDK 56 schema, read by no plugin — so "app.json is already correct, only the native manifest drifted" was wrong, and a prebuild would **not** have fixed it. The landed fix is `plugins/withCleartextTraffic.js` plus the patched manifest.
> - Ground truth 1 was already stale at execution time: `app.json` carried `ios.bundleIdentifier`, and `GroceryApp/ios/` was a full tracked Xcode project — not one file.
> - The upload-blocking constraint is satisfied: `dist-android/` and the assets bundle are untracked and purged from history.
> - `npm ci` was already reproducible (fixed at `8b9bc98`); the remaining lockfile work was deleting `yarn.lock` and pinning `packageManager`.
> - The invite-redirect defect is real but its path is wrong here: the only copy is `GroceryApp/relay-server/public/invite-redirect.html`, and the wrong-package URL is still unfixed there.
> - The Jest baseline is 44 suites / 499 passing, not 470.

## 1 · Session prompt

> Paste everything between `<setup>` and `</handoff>` below — the whole block, including the XML tags.

<setup>
> **[Erratum added 2026-07-29 — not part of the original prompt.]** This goal was COMPLETED on 2026-07-28. If you have been asked to execute this prompt, stop and confirm with the owner first — see the corrections notice at the top of `launch-goals/G4-produce-signed-installable-builds-for-both-sto.md` and `GOAL_PROMPT_NOTES.md` at the repository root.

The owner has assigned you one specific repository and branch, and three separate checkouts of this project exist. Work only inside the working copy you were given: do not search the filesystem for another checkout, and do not read or write anything outside the repository root you establish below.

Before you plan anything, establish the ground you are standing on, and state each answer in the conversation.

1. **Find the repository root.** Run `git rev-parse --show-toplevel`. Every path in this prompt is relative to that directory; there are no absolute paths here, and you should not assume any particular folder name above the root. If you are not inside a git repository, stop and ask the owner where the checkout is.
2. **Identify the branch that carries the launch work.** Do not assume the default branch does. On this project the launch work has historically lived on a feature branch while `main` stayed behind, and an earlier audit wasted an entire pass on the wrong one. Run `git status -sb`, `git branch -a`, and `git log --oneline -10`, then compare candidates with `git log --oneline main..<candidate>`. The launch branch is the one carrying the recent app work — Expo and React Native changes, relay changes, and a version around 1.30.x. If two candidates both look plausible, or the difference is not obvious, ask the owner which branch is the launch candidate and wait for an answer before editing anything. Work on that branch and nowhere else.
3. **Locate the two projects.** The Expo app is in `GroceryApp/` and the Node relay is in `relay-server/`, both relative to the repository root. Confirm both exist before continuing.
4. **Choose a scratchpad directory outside the repository** and note its path. Several steps below deliberately write outside the working copy: a throwaway keystore, an exported JavaScript bundle, and a copy of the app used for a trial prebuild. Nothing in this goal should leave a new file inside the repository unless you intended it.
5. **Note the environment check in ground truth 7 below.** It is the first real work of this goal, not a formality: what you can build here decides which of the items below you finish yourself and which become an owner handoff.
</setup>

<role>
You are the release engineer for PantryRun, a family grocery-list app (React Native 0.85, Expo SDK 56, WatermelonDB + Yjs CRDT + libsodium E2EE, plus a Node relay). You have a terminal in your agent session, but you hold no store credentials and will never be given any, and you do not yet know what this particular machine can build. Your job is to take the repository from "an iOS build cannot even be configured" to "the owner can produce a signed, store-uploadable build in one sitting" — and to prove every repo-side step with pasted command output rather than assertion.
</role>

<context>
This is goal 4 of 7 in a launch decomposition. Its subject is buildability: the app currently cannot be built for iOS at all, cannot reach its own relay from an Android or iOS release build, cannot install its dependencies in CI, and has no working crash reporting. Nothing else in the launch can be verified on a device until this is fixed.

**Where to work.** Use the branch you identified in setup step 2. Your working copy may carry uncommitted work from a sibling goal — in the audited copy this was a set of persistence fixes touching `GroceryApp/.gitignore`, `GroceryApp/__mocks__/watermelondb.ts`, `GroceryApp/src/crypto/index.ts`, `GroceryApp/src/identity/family.ts`, `GroceryApp/src/screens/PairingScreen.tsx`, `GroceryApp/src/storage/hydrate.ts`, `GroceryApp/src/storage/models.ts`, `GroceryApp/src/sync/bootstrap.ts` and `GroceryApp/src/voice/siri.ts`, plus an untracked `GroceryApp/__tests__/fresh-install-persistence.test.ts`. Run `git status --short` before you touch anything and record what you actually see. Do not match an exact file count and do not assume that work is present at all; if it is, it is not yours to modify. The quality gate you must not regress is `npx tsc --noEmit` clean and a Jest run at or above the count you measure at the start — the audited baseline was 470 passing, so measure yours before you change anything and compare against your own number.

**Supporting documents, if your working copy has them.** Several of these were written into a different working copy of this project, so check before relying on any of them, and ask the owner for a copy if a decision hinges on one. Everything this goal actually depends on is stated inline below, so a missing document should slow you down but never block you.
- `LAUNCH-PUNCH-LIST.md` at the repository root — 73 deduplicated defects, P0–P3, with file and line references. The entries belonging to this goal are the two critical build items C6 and C7 (no iOS bundle identifier, and cleartext traffic missing from the Android release manifest), the two P1 "Build" entries (the broken `npm ci` and the stub `eas.json`), and the two P2 entries covering the tracked debug keystore and the malformed `autoVerify` intent filter. Locate them by content rather than by line number, since line numbers drift.
- `PRE-LAUNCH-AUDIT.md` at the repository root — the same findings with evidence.
- `audit-package/02-APPLE-READINESS.md` and `audit-package/03-GOOGLE-READINESS.md` — per-store checklists that already distinguish repo work from owner work.
- `GroceryApp/docs/DEEP-LINK-HOSTING.md` — the association-file hosting and verification procedure, already written.
- `GroceryApp/docs/STORE_COMPLIANCE.md` — export-compliance answers and the console form content.
- `GOAL_PROMPT_NOTES.md` at the repository root — the project's running decision log. **Update it in place as you work**, and create it if it does not exist in your copy. It is the only mechanism that carries state between sessions; a future session will have zero memory of yours.

**Ground truth, verified directly against this repository on 2026-07-27.** Your working copy is a separate clone and may have moved since. Re-verify anything you are about to change — these are a snapshot, and you own what you edit.

1. `GroceryApp/app.json` has no `ios.bundleIdentifier`, no `ios.buildNumber`, and no `android.versionCode`. Because the identifier is absent, `npx expo config --type prebuild` resolves `bundleIdentifier: 'com.placeholder.appid'` — Expo injects a placeholder rather than erroring, so a prebuild run now would bake that placeholder into the Xcode project. `GroceryApp/ios/` contains exactly one file, `apple-app-site-association` — there is no Xcode project. iOS is unconfigurable today.
2. `GroceryApp/android/` **is** checked in — roughly 118 tracked files, including the native project, `app/debug.keystore`, and a stale `app/src/main/assets/index.android.bundle`. The checked-in `GroceryApp/android/app/src/main/AndroidManifest.xml` predates `app.json`'s Android settings. Note the direction of this defect carefully, because it points at the fix: `app.json` already sets `usesCleartextTraffic: true` and is **correct**; the tracked native manifest simply never received it, because it was generated before that key existed and has not been regenerated since. Its `<application>` tag carries no `android:usesCleartextTraffic` attribute, and its `autoVerify` intent filter mixes the `groceryapp` custom scheme with `https` in a single filter, while `app.json` declares them as two separate filters. The default relay URL is `ws://localhost` in `GroceryApp/src/config/settings.ts`, so a release build cannot reach any plaintext relay on Android. iOS is missing both halves of the equivalent: `app.json`'s `ios.infoPlist` holds only the four usage strings and `ITSAppUsesNonExemptEncryption`, so there is no `NSAppTransportSecurity` exception and no `NSLocalNetworkUsageDescription`, and on iOS 14+ a LAN relay needs both or the connection fails silently with no user-visible error.
3. `npm ci` fails. Reproduced in a clean directory from the committed `GroceryApp/package.json` and `GroceryApp/package-lock.json`: `npm error Missing: expo-image-picker@56.0.22 from lock file`. `expo-image-picker` is declared in `package.json` as `~56.0.20` and appears 0 times in `package-lock.json`. A second, divergent `yarn.lock` is also tracked and *does* contain it. No package manager is pinned.
4. Sentry is dead in release builds for two independent reasons, and untangling it has a trap. `GroceryApp/src/services/sentry.ts` reads `process.env.SENTRY_DSN` — without the `EXPO_PUBLIC_` prefix the Expo bundler never inlines it, so `SENTRY_DSN` is empty at runtime and `initSentry()` takes its "skipping init" branch. The prefix is used correctly elsewhere in the codebase, so this is an outlier rather than a project convention. Separately, three SDK versions coexist: root `@sentry/react-native@8.14.0`, `sentry-expo@7.2.0`, and `sentry-expo`'s own pinned `@sentry/react-native@5.17.0` nested under `GroceryApp/node_modules/sentry-expo/node_modules/`. **The trap:** `sentry-expo` is not merely a dependency. It is `app.json` `plugins[0]`, and `GroceryApp/src/services/sentry.ts` imports it directly. If you drop the package without also moving the plugin entry and the import, `npx expo config` and `npx expo prebuild` will both fail on an unresolvable config plugin, which takes down your evidence for items 2 and 7 as well. Establish the correct replacement package and plugin name for Expo SDK 56 from the installed packages and current Sentry documentation rather than from memory.
5. `GroceryApp/eas.json` is a stub: `production: {}`, `submit.production: {}`, no env block, no build type, no `appVersionSource`. Nothing about a production build is actually configured.
6. `.github/workflows/ci.yml` triggers only on push to `main` and on pull requests targeting `main`. It runs `npm ci`, so any pull request from the launch branch to `main` fails at install before it reaches `tsc` or Jest. Confirm both the triggers and the install step in your copy before you reason about when CI will break.
7. **Establish what this environment can actually build before you plan — check each item, do not assume it.** The point of this list is that the honest answer differs per machine, and both possible mistakes are expensive: attempting an iOS build on a machine that cannot do one wastes the session, while reporting a toolchain as missing when it is merely unwired turns a two-minute environment export into a false owner blocker.
   - **Operating system.** Run `uname -s` (or check the platform your session reports). If it is anything other than `Darwin`, iOS work is impossible in this environment regardless of everything else: Xcode, `xcodebuild`, `simctl` and CocoaPods exist only on macOS. In that case, say so plainly, complete the Android and repository-side work in full, and hand every iOS *build* step to the owner. Note that `npx expo prebuild --platform ios --no-install` is pure JavaScript and still runs on Linux or Windows, so done-when item 7 remains achievable off macOS even though a compiled iOS binary does not.
   - **Xcode.** Run `xcode-select -p`. A path ending in `CommandLineTools` means only the Command Line Tools are installed: `xcodebuild` will error with "requires Xcode, but active developer directory is a command line tools instance", and no iOS build or simulator run is possible. A path inside an `Xcode.app` bundle means a full Xcode is present. Confirm with `xcodebuild -version` and, for simulators, `xcrun simctl list devices available`. If only Command Line Tools are present, report that plainly and treat every iOS *build* step as an owner handoff rather than attempting a workaround; installing Xcode requires an Apple ID sign-in and a large download, so it is owner work.
   - **CocoaPods and the EAS CLI.** Run `pod --version` and `eas --version`. If either is absent, record it as an owner install. Do not attempt to install or authenticate them yourself; `eas` in particular is useless to you because every meaningful command requires a login you must never perform.
   - **A JDK.** Run `java -version` and `keytool` and read the output carefully rather than acting on the first line. On macOS, `/usr/bin/java` and `/usr/bin/keytool` are stubs that print "Unable to locate a Java Runtime"; that message means nothing was found on the default search path, **not** that no JDK exists on the machine. Before concluding a JDK is missing, check `$JAVA_HOME`, `which -a java javac keytool`, `/usr/libexec/java_home -V` on macOS, and the JetBrains Runtime bundled inside Android Studio, which typically lives at `Android Studio.app/Contents/jbr/Contents/Home` under `/Applications` on macOS, `jbr/` inside the Android Studio install directory on Linux, and `jbr\` under the Android Studio program directory on Windows. Use that JDK's own `bin/keytool`, not a stub on `PATH`. This matters because Gradle is a JVM program: with no JDK the wrapper cannot start at all, so every signing check in this goal is blocked at the first command — and if a usable JDK is sitting inside Android Studio, "no JDK installed" is a wrong blocker that costs the owner a pointless install.
   - **The Android SDK.** Check `$ANDROID_HOME` and `$ANDROID_SDK_ROOT` first, then the conventional locations: `~/Library/Android/sdk` on macOS, `~/Android/Sdk` on Linux, `%LOCALAPPDATA%\Android\Sdk` on Windows. Confirm with `ls "$ANDROID_HOME/build-tools"` and `ls "$ANDROID_HOME/platforms"` that build-tools and a platform matching the project's compile SDK are installed, and that `apksigner` exists under one of the build-tools directories. This matters because Gradle resolves the SDK from `sdk.dir` in `GroceryApp/android/local.properties` or from `ANDROID_HOME`; with neither set the release build fails during configuration, long before it reaches signing, and the error names neither cause clearly.
   - **Wiring, if the pieces are present but unset.** Export `JAVA_HOME` to the JDK you located and `ANDROID_HOME` to the SDK you located, and write `sdk.dir=<absolute path of the SDK you located>` into `GroceryApp/android/local.properties`. Check that `GroceryApp/android/.gitignore` lists `local.properties` — it did in the audited copy — so the file will not pollute `git status`; delete it when you are finished either way.
   - **Report before you plan.** State, in one short block, what you found for each of the above. If a genuine gap remains after you have wired up everything that is present, name it with the exact command, the exact error, and the precise install the owner must perform.
8. **What the working tree will and will not hide.** `GroceryApp/android/.gitignore` ignores `build/`, `.gradle` and `local.properties`, and `GroceryApp/.gitignore` ignores `dist/`, so a Gradle release build and a default `expo export` leave no untracked files behind. But `GroceryApp/.gitignore` ignores `*.jks` and **not** `*.keystore`, and `GroceryApp/android/app/debug.keystore` is tracked — so a keystore created inside the working copy would be stageable and could be committed by accident. Confirm both `.gitignore` patterns yourself; that asymmetry is why the scratchpad rule below is load-bearing rather than ceremonial.

**Corrections to the source documents.** Several things the punch list and the older single-prompt version list as outstanding are already done on the launch branch. Verify each yourself, then say so plainly rather than redoing the work:
- Android version coherence is already correct: `GroceryApp/android/app/build.gradle` reads `versionCode 30` and `versionName "1.30.0"`, matching `app.json`'s `1.30.0`.
- Release signing already reads the `PANTRYRUN_UPLOAD_*` gradle properties, guarded by a `project.hasProperty` check, so the wiring exists and does not need writing. The residual defect is narrower than "signing is wrong": the `signingConfig` falls back to `signingConfigs.debug` when the property is absent, so a local `assembleRelease` silently produces a debug-signed artifact.
- `ios.associatedDomains: ["applinks:groceryapp.app"]` is already present in `app.json`.
- `GroceryApp/ios/apple-app-site-association` and `GroceryApp/android/assetlinks.json` already exist as deploy templates with documented placeholders (`TEAMID` and `REPLACE_WITH_SHA256_FINGERPRINT_OF_YOUR_SIGNING_CERT`). They need values and hosting, not authoring.
- The punch list claims `app.json` holds a different, wrong value for the iOS identifier. That is inaccurate: `app.json`'s `ios` block contains no identifier at all. The wrong value `com.groceryapp.app` lives in stale documentation — `GroceryApp/docs/deep-linking-setup.md`, in several places. Fix or delete those lines so nobody copies them into a store console.
- The punch list and the older prompt both say the stale lockfile means CI "fails on every push". CI only triggers on `main`, so it has never run against the launch branch; the failure is real but arrives at merge time.
- One adjacent defect you should record but **not** fix here: `relay-server/public/invite-redirect.html` sets `androidStoreUrl` to a Play listing for `com.groceryapp.app`, the wrong package, so every Android user who taps an invite link without the app installed lands on a nonexistent Play listing. It is web content, not build configuration, so it belongs to a sibling goal. Add it to `GOAL_PROMPT_NOTES.md` and to the punch list if you have one.

**The bundle identifier is settled; do not reopen it.** Use `com.shiftlogichq.pantryrun`, matching the Android package (`app.json` `android.package`, and `applicationId` and `namespace` in `build.gradle`) and the existing AASA template. There is an open owner decision about a friendlier *display* name (CartNest, SnugCart, KinCart) recorded in the store-listing section of the audit package; it affects `expo.name` and listing copy only. The bundle identifier is user-invisible and can stay, and the invite-link domain `groceryapp.app` is brand-neutral and keeps working, so neither open question blocks you. Flag to the owner that the bundle identifier becomes permanently immutable the moment the App Store Connect record is created, and that this is the last cheap moment to object.
</context>

<objective>
Bring the repository to the state where the owner, once they supply the credentials only they can supply, can produce a signed store-upload artifact for both platforms without any further engineering. Concretely: make the dependency install reproducible, give iOS an identity and a generatable native project, make the relay reachable from a release build on both platforms, prove the Android release signing path actually signs with the upload key rather than the debug key, get Sentry inlining a real DSN into the emitted bundle, fill in the EAS production configuration, and prepare the deep-link association files right up to the two values the owner must provide.

Every claim in your final report must be backed by output you pasted into the conversation. "I read the file and it looks right" is not evidence for any item here — reading `sentry.ts` cannot tell you whether the DSN reaches the bundle, and reading `build.gradle` cannot tell you which certificate signs the artifact.
</objective>

<constraints>
**Never authenticate to anything with a credential found in this repository or its history.** Two live read-write Turso tokens and the blind-RSA issuer private key are permanently in git history. During an earlier pass, an audit agent extracted the Turso token and queried the production database. Do not repeat that — not to confirm a token is live, not to test a code path, not for any reason. Rotation does not make this safe: the token shipped as a client-side fallback (`settings.tursoToken || '<literal>'`), so any replacement shipped the same way is extractable from the APK by anyone who downloads it.

**Do not produce an artifact intended for upload until the token-bearing files are gone.** `GroceryApp/.easignore` excludes `node_modules`, `node_modules_bak`, `__tests__`, `docs`, `references`, `scripts`, `.git`, `.gitignore`, `*.md`, `*.log`, `.turso`, `.env`, `*.bak` and `*.bak2` — it does **not** exclude `GroceryApp/dist-android/` or `GroceryApp/android/app/src/main/assets/index.android.bundle`. Both are tracked, both carry a live Turso token, and the second sits inside the Android release source set. Verify this is still true in your copy. If you build an upload candidate before that cleanup lands — it belongs to the credentials goal, not this one — the very first signed artifact ships a live production credential to the world. Local wiring tests are fine and expected; an upload candidate is not. If the cleanup has not happened by the time you finish, say so as a blocking dependency rather than working around it.

**The signing proof must use a disposable keystore, and that keystore must never be committed.** To prove `signingConfigs.release` is wired correctly you need to actually sign something, which means generating a keystore. Generate it in the scratchpad directory you chose in setup — a path outside the repository — under a name that cannot be mistaken for the real one: `throwaway-signing-test.keystore`. Use the `bin/keytool` belonging to the JDK you located in ground truth 7, not a stub `keytool` on `PATH`. Pass the keystore via `-PPANTRYRUN_UPLOAD_STORE_FILE=` on the command line rather than writing it into any tracked `gradle.properties`, and delete it when the check is done. Then paste `git status --short` for the whole working copy: the point is not that the scratchpad file is gone (it was never inside the repository, so it could never have appeared there) but that the release build left no new untracked artifact anywhere in the tree. Recall from ground truth 8 that `*.keystore` is **not** git-ignored — a keystore written into `GroceryApp/` would be stageable, which is precisely the accident this rule prevents. A leaked keystore is a permanent compromise of app-update authenticity, and the real upload keystore is an owner deliverable you must never generate or hold. State that distinction explicitly in your report so the owner does not mistake the throwaway for the real one.

**`npx expo prebuild --clean` against the tracked `GroceryApp/android/` is destructive and requires explicit owner confirmation before you run it.** The tracked `build.gradle` carries hand-written edits — the `PANTRYRUN_UPLOAD_*` signing block and `versionCode 30` — that a clean prebuild will overwrite. Worse, `app.json` declares no `android.versionCode`, so a regenerated `build.gradle` would emit `versionCode 1` and silently destroy the version coherence the audit currently credits this branch with. There are three honest ways forward and you must pick one deliberately, record the choice and its reasoning in `GOAL_PROMPT_NOTES.md`, and only then execute:
  (a) Hand-patch the checked-in manifest for cleartext and split the `autoVerify` intent filter. Minimal and safe, but it leaves `android/` permanently divergent from `app.json` — which is the exact mechanism that produced this defect in the first place, and it will produce it again.
  (b) Add the missing keys to `app.json` (`android.versionCode: 30`, `ios.bundleIdentifier`, `ios.buildNumber`), prebuild, then re-apply the signing block. Converges the two sources of truth; higher risk in one step.
  (c) Untrack `android/` entirely and go full Expo continuous-native-generation. Arguably the correct end state, and it removes the tracked `debug.keystore` and the token-bearing bundle as a side effect — but it is destructive, it overlaps the credentials goal, and it must not happen without the owner saying yes.
Whichever you choose, `app.json` should end up carrying `android.versionCode` and `ios.buildNumber` explicitly, because a config that only works as long as nobody regenerates the native projects is not a fix.

**Do the iOS native-project check in a scratchpad copy, not in the working copy.** `npx expo prebuild --platform ios --no-install` generates the Xcode project and Podfile without needing Xcode or CocoaPods, and it runs on any operating system. Run it against a copy so that `GroceryApp/ios/` in the working copy keeps containing only the AASA file. `GroceryApp/` is large — roughly 800 MB in the audited copy, of which `node_modules/` was about 768 MB — and prebuild needs `node_modules` present to resolve the four config plugins. Use a clone-on-write or hard-link copy where the filesystem supports it (`cp -Rc` on APFS, `cp -al` on most Linux filesystems), since either is near-instant and consumes almost no additional disk. If neither is available, `rsync -a --exclude node_modules` into the scratchpad and then symlink `node_modules` in. Whether the generated Xcode project should ultimately be committed is a real decision — bare workflow versus continuous native generation — and it is the owner's, not yours. The reason to check at all is that a config error in `app.json` is invisible until something consumes it; the generated `Info.plist` and `.pbxproj` are the first consumers.

**Do not change what is not yours.** The persistence work, the recovery-phrase first-run moment, the Trip Optimizer gating, the credential rotation and history rewrite, and the store-listing copy all belong to sibling goals. When you find something new and real, add it to `GOAL_PROMPT_NOTES.md` and the punch list instead of quietly widening a change. A large diff that touches five goals cannot be reviewed or reverted independently.

**A green test against a mock is not evidence.** This suite passed 465 tests while the app persisted nothing, because `__mocks__/watermelondb.ts` was more permissive than the real library. That specific bug is fixed, but the habit that caused it is what you are guarding against. For everything in this goal the real check is cheap — run the install, emit the bundle, sign the artifact, read the generated plist — so there is no excuse for inferring.

**Do not stop early on token-budget grounds.** Context compacts automatically. Stopping mid-way leaves the repository in a half-migrated state that is worse than either endpoint, because the next session cannot tell which half is intentional. If you must pause, land a coherent checkpoint and write down exactly where you are in `GOAL_PROMPT_NOTES.md` first.

**Keep `GOAL_PROMPT_NOTES.md` current as you go, not at the end.** Add one dated section for this goal. One entry per decision or lesson; update entries in place rather than appending near-duplicates. A session can end without warning, and an unwritten decision is a decision the next agent will re-litigate from scratch.
</constraints>

<done_when>
Each item below is satisfied only when you have pasted the referenced output into the conversation. Report the real numbers, including any that disappoint. Items 2 and 5 are coupled: do the Sentry work first, then produce the item-2 evidence, because a Sentry change that touches `app.json` invalidates an earlier `expo config` paste.

1. **`npm ci` reproduces.** In a clean directory containing only the committed `GroceryApp/package.json` and `GroceryApp/package-lock.json`, `npm ci` completes with no "Missing … from lock file" error. Paste the failing "before" line and the succeeding "after" tail. State which lockfile is now authoritative and whether `yarn.lock` was removed or kept, with the reason.
2. **The resolved config carries an iOS identity and explicit version codes, and still resolves.** `npx expo config --type prebuild`, run **after every other change in this goal has landed**, exits successfully and shows `bundleIdentifier: 'com.shiftlogichq.pantryrun'` (replacing today's injected `'com.placeholder.appid'`), an `ios.buildNumber`, and `android.versionCode: 30`. Paste the relevant slice. That this command still resolves at all is half the evidence: if the Sentry rework dropped `sentry-expo` without relocating the `app.json` plugin entry, it would fail here on an unresolvable plugin.
3. **Cleartext reaches the Android release build.** `grep -n usesCleartextTraffic GroceryApp/android/app/src/main/AndroidManifest.xml` shows the attribute on the `<application>` element, and the `autoVerify` intent filter contains only the `https` scheme, with the `groceryapp://` scheme moved to its own filter — mixing a custom scheme under `autoVerify` can fail App Links verification outright. Paste both. The iOS half of this defect is proven in item 7, not here: `expo config` merely echoes back what you typed into `app.json`, so it can only show that you added a key, never that the key survives into a built app.
4. **Release signing is proven, not assumed.** Using a scratchpad `throwaway-signing-test.keystore`, an Android release bundle is produced and its signer certificate printed (`keytool -printcert -jarfile <artifact>` or `apksigner verify --print-certs`), showing the throwaway certificate and **not** `CN=Android Debug`. Then the keystore is deleted and `git status --short` is pasted for the whole working copy, showing that the release build left no new untracked file anywhere in the tree. This item depends on the environment check in ground truth 7, and how you satisfy it depends on what you found there. If a JDK and the Android SDK are present — including a JDK bundled inside Android Studio, which is easy to miss because the platform stub `java` reports "Unable to locate a Java Runtime" — then wire up `JAVA_HOME`, `ANDROID_HOME` and `GroceryApp/android/local.properties` and produce a real signature; a stub's complaint about a missing runtime is not a blocker and does not satisfy this item. If a JDK or the SDK is genuinely absent after you have looked in every location listed in ground truth 7, then the item is satisfied instead by a specific named blocker: the exact command, the exact error, and the precise install the owner must perform, stated after you have set all three of `JAVA_HOME`, `ANDROID_HOME` and `local.properties` to whatever does exist.
5. **Sentry actually reaches the bundle.** Rename the DSN environment variable to `EXPO_PUBLIC_SENTRY_DSN` everywhere it appears (`GroceryApp/src/services/sentry.ts`, `GroceryApp/.env.example`, `GroceryApp/eas.json`, and any documentation) and resolve the three-way SDK version conflict to a single supported combination — remembering that `sentry-expo` is `app.json` `plugins[0]` and is imported by `src/services/sentry.ts`, so all three move together or none do. Then prove the inlining with a **unique sentinel**: export with a DSN containing a string that could not occur naturally, such as `https://sentinel0000@o0.ingest.sentry.io/0`, run `npx expo export --platform android --output-dir <scratchpad path>` so the emitted tree lands outside the repository, and search it for the literal `sentinel0000` using `grep -ar` (or `strings … | grep`). Paste the matching **filename** and the hit. Do not grep for the host `o0.ingest.sentry.io` — it is a plausible substring of unrelated bundled code, so a hit would prove nothing. If the minified Hermes bytecode gives an ambiguous result, re-export with `--no-minify` (or a dev export), where the string is plainly readable, and say which variant produced your evidence. Also paste `npm ls @sentry/react-native`.
6. **`eas.json` is a real production configuration.** Paste the file. It declares the production Android build type as `app-bundle`, an `env` block naming `EXPO_PUBLIC_SENTRY_DSN`, and `cli.appVersionSource: "local"`. Choose `local` deliberately, not `remote`: `remote` moves version-code ownership onto EAS's servers, which would contradict the explicit `android.versionCode: 30` you set in item 2 and would require an authenticated EAS project you cannot reach. `local` keeps the version number in git where the diff is reviewable.
7. **The iOS native project generates cleanly, and carries the network keys.** `npx expo prebuild --platform ios --no-install` in a scratchpad copy succeeds. This needs neither Xcode nor CocoaPods and runs on any operating system, so a missing iOS toolchain — or a non-macOS host — is not a reason to skip it. Paste, from the **generated** files: `PRODUCT_BUNDLE_IDENTIFIER = com.shiftlogichq.pantryrun` from the `.pbxproj`, and from the generated `Info.plist` both the `NSAppTransportSecurity` exception scoped to local and self-hosted relays and the `NSLocalNetworkUsageDescription` string. This generated plist — not `app.json`, not `expo config` — is the artifact that proves the iOS half of the relay-reachability defect is fixed. The ATS exception is the expected path; do not "solve" this by switching the app to `wss://`, because a self-hosted relay on a LAN address cannot practically obtain a TLS certificate, so that change would remove the primary use case rather than harden it. Raise it with the owner if you think otherwise; do not do it unilaterally. Finally, confirm that `GroceryApp/ios/` in the working copy still contains only `apple-app-site-association`.
8. **The gate is still green.** `npx tsc --noEmit` reports 0 errors and Jest reports at least the passing count you measured at the start (470 in the audited copy). Paste both summary lines, and paste the starting count you are comparing against. If a count moved, explain why.
9. **`GOAL_PROMPT_NOTES.md` has a dated section for this goal** recording the prebuild-fork choice and its reasoning, the ATS decision, the Sentry version and plugin resolution, the lockfile decision, the environment and toolchain findings from ground truth 7, and every source-document correction you confirmed or found — including the wrong Play Store package in `relay-server/public/invite-redirect.html`, which you record but do not fix.
10. **One consolidated handoff block** (in your final message and mirrored into `GOAL_PROMPT_NOTES.md`) lists every remaining owner action as an exact command or an exact value to paste, batched so the owner can clear them in one sitting.
</done_when>

<handoff>
Stop at these boundaries. Prepare everything up to the edge, then present the batch — do not ping the owner one item at a time.

- **Apple Team ID.** Needed to replace `TEAMID` in `GroceryApp/ios/apple-app-site-association` before it is hosted, and required by any real iOS signing. You cannot obtain it; the owner reads it from their Apple Developer account.
- **iOS signing certificates and provisioning profiles.** Owner-only. Do not attempt `eas credentials`, `eas login`, or any Xcode signing flow — all of them are authentication.
- **The Android upload keystore.** The owner generates and backs it up; you supply the exact command. `GroceryApp/android/app/build.gradle` already documents it in a comment above the signing block: `keytool -genkeypair -v -keystore pantryrun-upload.keystore -alias pantryrun-upload -keyalg RSA -keysize 2048 -validity 10000`. Tell the owner where to store it, that losing it locks them out of Play updates unless enrolled in Play App Signing, and that it must never enter the repository — noting that `*.keystore` is not in `.gitignore` today.
- **The signing-certificate SHA-256** for `GroceryApp/android/assetlinks.json`. If the owner enrols in Play App Signing — recommended — the correct fingerprint is the one Play generates (Play Console → Setup → App integrity → App signing key certificate), *not* the upload key's. Getting this wrong makes verified App Links fail after Play re-signs.
- **Hosting both association files** at `https://groceryapp.app/.well-known/` as `application/json`, over HTTPS, with no redirects, and with the iOS file served without a `.json` extension and with the `_comment` key stripped. The procedure and the post-deploy verification commands are already written in `GroceryApp/docs/DEEP-LINK-HOSTING.md` if that file is present in your copy; if it is not, write the procedure out for the owner yourself.
- **Installing the iOS toolchain**, if the owner wants any local iOS build: a full Xcode (not just the Command Line Tools) and CocoaPods, on a Mac. This requires an Apple ID sign-in and a large download. Report what you actually found in ground truth 7 rather than assuming — and if a JDK and the Android SDK turned out to be present and merely unwired, say so explicitly, since environment wiring is your job and not an install the owner needs to perform.
- **Installing and logging into the EAS CLI**, and running any `eas build` or `eas submit`. All authentication.
- **The display-name decision** recorded in the store-listing section of the audit package (PantryRun versus CartNest, SnugCart or KinCart) and whether the invite-link domain stays `groceryapp.app`. Neither blocks your work, but both get materially more expensive after first submission — surface them now.
- **Confirmation before any destructive step**: the prebuild fork option (c), any `--clean` prebuild, and any force-push. Ask, wait for a clear yes, then act.
</handoff>

## 2 · `/goal` condition (3920 chars, limit 4000)

> If your agent session offers a goal or completion-condition command — Claude Code's `/goal`, for example — pass it the contents of this fenced block after sending the prompt above. If it does not, use the block as the acceptance checklist you report against at the end.

```
PantryRun is buildable and signable for both stores on the launch branch identified at session start, with every claim proven by pasted command output rather than by reading a file. The agent first reported what this environment can build — OS, whether xcode-select -p points at a full Xcode or only Command Line Tools, whether CocoaPods and the EAS CLI exist, and which JDK and Android SDK it located — and treated any genuine gap as an owner handoff, not a workaround. The agent has surfaced: (1) a clean-directory npm ci completing with no "Missing ... from lock file" error, alongside the original failure line naming expo-image-picker@56.0.22, plus a stated decision on yarn.lock; (2) npx expo config --type prebuild output, from a run made AFTER every other change, showing bundleIdentifier 'com.shiftlogichq.pantryrun' (not 'com.placeholder.appid'), an explicit ios.buildNumber, and android.versionCode 30 — sentry-expo is app.json plugins[0] and is imported by src/services/sentry.ts, so the Sentry rework must move the package, the plugin entry and the import together or this command fails; (3) a grep of android/app/src/main/AndroidManifest.xml showing android:usesCleartextTraffic="true" on the <application> element, with the autoVerify intent filter carrying only the https scheme and groceryapp:// in its own filter; (4) keytool -printcert -jarfile or apksigner verify --print-certs output for an Android release artifact built via -PPANTRYRUN_UPLOAD_STORE_FILE against a scratchpad throwaway-signing-test.keystore, showing that throwaway certificate and NOT CN=Android Debug, then keystore deletion and a git status --short showing no new untracked file — or, if a JDK or the SDK is genuinely absent, a named blocker with exact command, error and owner install, reported only after JAVA_HOME, ANDROID_HOME and android/local.properties were set to whatever exists there (a stub reporting "Unable to locate a Java Runtime" is not itself a blocker); (5) after renaming the DSN variable to EXPO_PUBLIC_SENTRY_DSN in src/services/sentry.ts, .env.example and eas.json, a named output file plus a grep -ar hit for a unique sentinel (e.g. sentinel0000) in a scratchpad --output-dir tree from npx expo export --platform android, naming the export variant (a grep for o0.ingest.sentry.io alone does not count), plus npm ls @sentry/react-native showing the sentry-expo 7.2.0 / @sentry/react-native 8.14.0 / nested 5.17.0 conflict resolved to one supported combination; (6) a filled GroceryApp/eas.json declaring an app-bundle production Android build, an env block naming EXPO_PUBLIC_SENTRY_DSN, and cli.appVersionSource "local", not "remote"; (7) from files generated by npx expo prebuild --platform ios --no-install in a scratchpad copy: PRODUCT_BUNDLE_IDENTIFIER = com.shiftlogichq.pantryrun in the pbxproj and both an NSAppTransportSecurity exception for self-hosted relays and an NSLocalNetworkUsageDescription string in the generated Info.plist (the app was NOT switched to wss://), with GroceryApp/ios/ still holding only apple-app-site-association; and (8) npx tsc --noEmit reporting 0 errors and Jest passing at or above the count measured before any change, with both numbers pasted and any movement explained. No repository or git-history credential was used against a live service; no upload-candidate artifact was produced while the token-bearing index.android.bundle and dist-android/ remain tracked; no keystore was committed; any destructive step (prebuild --clean, untracking android/, force-push) happened only after owner confirmation quoted in the conversation. GOAL_PROMPT_NOTES.md at the repository root has a new dated section recording the prebuild-fork choice, the ATS decision, the Sentry resolution, the lockfile decision, the environment findings and each source-document correction, and the final message carries one handoff block of exact commands and values for every owner-only item.
```
