# G5 — Set the v1 product posture: free tier and first-run key backup

## 1 · Session prompt

> Paste everything between `<setup>` and `</handoff>` below — the whole block, including the XML tags.

<setup>
The owner has assigned you one specific repository and branch, and three separate checkouts of this project exist. Work only inside the working copy you were given: do not search the filesystem for another checkout, and do not read or write anything outside the repository root you establish below.

Before anything else, establish these four facts for your agent session and state them back in the conversation. Everything after this depends on them.

**The repository root.** Run `git rev-parse --show-toplevel` and treat its output as the root. Every path in this brief is relative to that root, and no path here is absolute. The React Native app lives in the `GroceryApp/` subdirectory; unless a step says otherwise, run commands from `GroceryApp/`.

**Which branch carries the launch work.** Do not assume the default branch does. Run `git branch -a` and `git log --oneline -5` against the candidates, and confirm the branch you are on is the one holding the launch commits. As context only: in the working copy this brief was written against, that branch was named `claude/dreamy-faraday-758d4e`, its tip was `8dde4af0`, it was roughly eighteen commits ahead of `main` and unmerged, and `main` was stale — an earlier audit pass was wasted working on `main`. Your copy may use a different branch name and a different tip. If it is not obvious which branch carries the launch work, stop and confirm with the owner before you change anything.

**That cited line numbers have drifted.** Every `file:line` reference below comes from the working copy this brief was written against. Read the line number as a hint and the symbol name as the real identifier: locate code by grepping for the named symbol (for example `grep -n "MANAGED_TIER_ENABLED" src/screens/SettingsScreen.tsx`) rather than trusting the number. When a line number and a symbol disagree, the symbol wins.

**Your own build and test baseline.** From `GroceryApp/`, check that dependencies are installed — `ls node_modules/.package-lock.json` or `ls node_modules | head` will tell you. If `node_modules` is absent, run `npm install` and note that you did. Then run `npx tsc --noEmit` and `npx jest` *before* you change anything, and write down exactly what each reports. Those recorded numbers are your baseline; later steps compare against them, not against any figure quoted in this brief.
</setup>

<role>
You are a senior React Native engineer taking PantryRun — a family grocery-list app built on React Native 0.85, Expo SDK 56, WatermelonDB, Yjs CRDT and libsodium end-to-end encryption — through its final pre-launch pass. You have no memory of any earlier session, so treat this brief plus the files it names as your only ground truth, and verify any claim before you build on it.
</role>

<context>
This is Goal 5 of a 7-goal launch program. This goal decides what v1 actually is as a product: which surfaces a user can reach, and whether a user can survive losing their phone.

Working-copy state. Run `git status --short` before you change anything, and read the result rather than expecting a particular shape. The P0 persistence fixes — the change that made the app save anything at all — consist of edits to eight files plus one new test file: `GroceryApp/__mocks__/watermelondb.ts`, `GroceryApp/src/crypto/index.ts`, `GroceryApp/src/identity/family.ts`, `GroceryApp/src/screens/PairingScreen.tsx`, `GroceryApp/src/storage/hydrate.ts`, `GroceryApp/src/storage/models.ts`, `GroceryApp/src/sync/bootstrap.ts`, `GroceryApp/src/voice/siri.ts`, and `GroceryApp/__tests__/fresh-install-persistence.test.ts`. What matters is that this work is **present** in your working copy, not what `git status` marks it as. Verify presence by content and file existence — `GroceryApp/__tests__/fresh-install-persistence.test.ts` should exist, and `npx jest __tests__/fresh-install-persistence.test.ts` should run it. If those changes are uncommitted in your copy, treat them as someone else's unreviewed work and do not discard them; if they are already committed on the launch branch, that is fine and expected. Only if the work is missing entirely should you stop and tell the owner. Whatever `git status` shows will also include entries from other goals working in the same tree; extra modified or untracked entries are normal and are not a reason to stop. Some earlier documents describe this state as exactly "9 files" or as "8 modified and 1 untracked"; go by the named list, not by a count or a status marker.

Source documents. Read the ones that exist in your working copy rather than re-deriving them, and do not block on the ones that do not — every fact this goal actually depends on is stated inline in this brief.
- `LAUNCH-PUNCH-LIST.md`, if present in your working copy — a deduplicated defect list, P0 to P3, with `file:line` citations. Your entries there are two open follow-ups plus the free-tier posture; both follow-ups are described in full under objectives C and D below. If the file is absent, ask the owner for it or proceed without it.
- `PRE-LAUNCH-AUDIT.md`, if present — the evidence behind those findings. Absent, proceed on the evidence quoted here.
- `GOAL_PROMPT_NOTES.md` at the repository root — the project's running decision log and the cross-session state mechanism. It ran to about 127 lines when this brief was written; expect a different length. If it does not exist in your working copy, create it at the repository root.
- `GroceryApp/docs/MONETIZATION.md` — the owner's monetization decision and the post-v1 plan.

Owner decisions that are settled and must not be re-litigated. v1 ships fully free with no in-app purchase. Apple App Store Review Guideline 3.1.1 rejects apps that offer digital features which are not sold through In-App Purchase, so an un-buyable upgrade field or a visible paid feature is a rejection magnet rather than a harmless leftover. Trip Optimizer is the paid tier's hero feature for 1.x and must not ship in v1. Cloud voice-assistant linking stays disabled. There is no signup, no account, no email and no password anywhere in this app: identity is a per-device keypair in secure storage.
</context>

<objective>
Four pieces of work, all inside the repository you identified in `<setup>`.

**A. Gate Trip Optimizer off behind `TRIP_OPTIMIZER_ENABLED = false`.**

State to verify first: `GroceryApp/src/screens/GroceryListScreen.tsx` imports `StopOptimizer` (near line 37 when this brief was written) and renders it unconditionally (near line 1019). `StopOptimizer` owns the paid experience internally — it renders `TripPlanSheet` inside `GroceryApp/src/components/StopOptimizer.tsx` (near line 178), and that is the only `<TripPlanSheet` site in the tree — so gating the single `<StopOptimizer` JSX site removes the whole surface. Confirm both facts with `grep -rn "<StopOptimizer\|<TripPlanSheet" src/` before you edit. Do not add a second guard inside `StopOptimizer.tsx`: the const is module-local to `GroceryListScreen.tsx` and referencing it from another module would not compile. `<TripPlanSheet` is unreachable transitively, and that is the claim you prove — which is also why a naive grep asking for every paid render site to sit behind the flag can never be satisfied.

Follow the pattern used by `MANAGED_TIER_ENABLED` in `GroceryApp/src/screens/SettingsScreen.tsx` (declared near line 60, used near line 385), not the one near `:373`/`:538`. The file contains two different idioms and only the first is what you want: a module-level `const MANAGED_TIER_ENABLED = false;` preceded by a doc comment that says why it is off and what flipping it back on requires, guarding its JSX with a direct `{MANAGED_TIER_ENABLED && ( … )}`. The other idiom is transitive — `isSelfHosted` derives from the flag and gates the Managed Subscription block further down — which is harder to audit. Use the direct form.

Why hide it rather than ship it free: taking a feature away from users who already have it, to put it behind a paywall in 1.1, reliably costs ratings and generates refund requests. Never shipping it is clean; un-shipping it is not.

Do not delete anything. `GroceryApp/src/pricing/stop-optimizer.ts`, `GroceryApp/src/pricing/trip-plan.ts`, `GroceryApp/src/components/StopOptimizer.tsx`, `GroceryApp/src/components/TripPlanSheet.tsx` and `GroceryApp/__tests__/stop-optimizer.test.ts` all stay exactly where they are. The 1.x plan is to flip one flag and add an entitlement check; deleting the code forces a re-implementation and throws away the existing test coverage.

**B. Prove no paid surface is reachable and no purchase copy is live.**

Confirm that `MANAGED_TIER_ENABLED` and `VOICE_ASSISTANT_LINKING_ENABLED` in `GroceryApp/src/screens/SettingsScreen.tsx` (near lines 60 and 74) are still `false`, and that `ASSISTANT_INTEGRATION` is unset for the relay. Confirm the "Managed Subscription" section is genuinely unreachable: its JSX is guarded by `{!isSelfHosted && (` (near line 538; the line above it is only the section comment), and `isSelfHosted` (near line 373) is `!MANAGED_TIER_ENABLED || settings.hostingTier === 'self_hosted'`, which is unconditionally true while the flag is false. State that reasoning explicitly rather than asserting the conclusion.

Then sweep the app source for purchase-implying copy: premium, upgrade, subscribe, subscription, paywall, unlock. A case-insensitive grep of those words over `GroceryApp/src/` and `GroceryApp/App.tsx` returned 27 hits across 11 files in the working copy this brief was written against. Your count may differ, and a different count is information rather than a failure — what matters is that every hit you get is named and placed in one of these categories, or removed:

- BIP39 wordlist entries: `unlock` and `until` in `GroceryApp/src/identity/recovery.ts` (near `:238`), `update` and `upgrade` on the following line (near `:239`).
- Price-normalizer stopword lists that contain `premium`: `GroceryApp/src/pricing/store-prices-adapter.ts` (near `:51`), `GroceryApp/src/pricing/flipp-deals-adapter.ts` (near `:43`), `GroceryApp/src/services/dealMatcher.ts` (near `:53`).
- The React Navigation and notification-listener senses of `subscribe` / `subscription`: `GroceryApp/src/navigation/deepLinks.ts` (near `:69`–`:70`) and `GroceryApp/src/navigation/useNotificationNavigation.ts` (near `:19`, `:38` and `:42`).
- `upgrade` meaning a schema version upgrade: `GroceryApp/src/storage/migrations.ts` (near `:2`).
- `unlock` meaning "decrypt with your own key" in user-facing copy: `GroceryApp/src/screens/SettingsScreen.tsx` (near `:252` and `:492`) and `GroceryApp/src/screens/PairingScreen.tsx` (near `:134`).
- Managed-tier plumbing that stays because the tier is gated off: `GroceryApp/src/types/index.ts` (near `:169`); `GroceryApp/src/config/settings.ts` (near `:41`, `:222`, `:224` and `:227`); and the managed-tier block in `GroceryApp/src/screens/SettingsScreen.tsx` (near `:8`, `:55`, `:537`, and `:540` through `:545`).

Be careful with that last group. Inside the managed-tier block, `SettingsScreen.tsx` renders the section title "Managed Subscription", the field label "Subscription Key", and the placeholder "Enter your subscription key" on consecutive lines (near `:540`, `:542` and `:545`). Those are literal purchase strings in the source and they are the hits most likely to alarm you. They stay: they sit inside the `{!isSelfHosted && (` block that cannot render, and deleting them would rip out plumbing that item A's sibling decision says to keep for 1.x. Explain them; do not remove them.

How to check reachability — and how not to. Do **not** try to prove non-reachability by exporting a bundle and grepping it. Metro does not tree-shake, so `TripPlanSheet`'s strings will still appear in an exported bundle even with the flag off, and you will burn a session concluding nothing. The provable checks are static and test-based: greps over `src/` showing every render site and its guard, plus a source-scanning Jest test. `GroceryApp/__tests__/ac15-no-analytics.test.ts` is the in-repo precedent for a test that reads source text and asserts a pattern is absent — copy its shape.

One suite to expect: `GroceryApp/__tests__/ac6-tier-switch.test.ts` exercises `setHostingTier` and the managed/self-hosted switch at the settings level, not the UI gate. If you alter the `isSelfHosted` derivation it is the suite that will break; a correct change that only adds a UI gate should leave it green.

**C. Give the recovery phrase a real first-run moment. This is launch-blocking.**

Behaviour to verify: on first launch `bootstrapSync()` in `GroceryApp/src/sync/bootstrap.ts` finds no master key and calls `provisionFirstRun()`, which founds a family-of-one and calls `generateRecoveryPhrase()` in `GroceryApp/src/identity/recovery.ts` (near `:407`). That mints twelve BIP39 words, derives the master key from them, and writes both to secure storage. `bootstrapSync` is invoked from `GroceryApp/App.tsx` (near `:161`). The user is never shown the phrase and is never told it exists. The only way to see it is Settings → Security → the "🔐 View Recovery Phrase" button in `SettingsScreen.tsx` (near `:688`), whose confirm action navigates to `RecoveryScreen` in `mode: 'show'`. Note the label is "View Recovery Phrase"; some documents call it "Show recovery phrase", and that string does not exist in the code.

In an end-to-end encrypted app with no accounts there is no server-side reset. That phrase is the only path back to a user's data after a lost or wiped phone. Shipping without telling the user it exists means the first support ticket is unanswerable and the data is gone.

Add a first-run step that shows the phrase and asks the user to save it. Requirements, each with its reason:

- It must not be a modal people reflexively dismiss. Follow the shape of `contributeConsentShown` in `DEFAULT_SETTINGS` (`GroceryApp/src/config/settings.ts`, typed in `GroceryApp/src/types/index.ts`): a persisted acknowledgement flag. Deliberately do **not** repeat the bug in that same flow: `ContributeConsentModal`'s `onCancel` and `onSkip` handlers in `SettingsScreen.tsx` (near `:965` and `:970`) both call `handleUpdate({ contributeConsentShown: true })`, so dismissing the modal permanently suppresses it. (The settings toggle, near `:785`, only opens the modal; the defect is in the two dismiss handlers.) For your flag, dismissing or backgrounding must leave it unset so the prompt re-surfaces, and only an explicit "I've saved it" acknowledgement may set it.
- Reuse `RecoveryScreen` (`GroceryApp/src/screens/RecoveryScreen.tsx`, registered in `GroceryApp/App.tsx`) rather than duplicating phrase-display code, so there is one place where key material is rendered. Be aware that `mode: 'show'` contains a live throw-path: when `getStoredRecoveryPhrase()` returns null it falls through to `generateRecoveryPhrase()`, which throws on its own `hasMasterKey()` guard. On the first-run happy path a phrase is stored so this does not fire, but it is the same defect described in the next bullet. If that fix lands the fallback becomes dead code and should raise an actionable error instead of an unexplained one; if the fix does not land, your first-run flow must not route through that fallback.
- The copy must be honest that losing the phrase means losing the data, and must not promise that this phrase is permanent — a device that later joins another family gets re-keyed by `PairingScreen`, and the first-run phrase stops being the one that matters.
- Verify and, if it holds, fix a related defect that appears in no source document. `recoverFromPhrase()` in `GroceryApp/src/identity/recovery.ts` (near `:480`) calls `setMasterKey()` on success but, unlike `generateRecoveryPhrase()`, never calls `setRecoverySeed(familyId, entropy)` and never writes the phrase or the stored-flag under the joined `familyId`. As a result, on every device that joined a family rather than founding one, `hasRecoveryPhrase()` returns false and `getStoredRecoveryPhrase()` returns null, so Settings → View Recovery Phrase falls through to `generateRecoveryPhrase()`, which throws "Cannot generate recovery phrase after data has been encrypted." A first-run promise to show the user their phrase is dishonest if the retrieval path errors for every non-founding device. Check whether adding the seed and phrase writes on the success path of `recoverFromPhrase()` leaves `GroceryApp/__tests__/ac13-key-recovery.test.ts` and `GroceryApp/__tests__/join-flow.test.ts` green. If it does, ship it. If it does not, do not force it — record what broke in `GOAL_PROMPT_NOTES.md`, add it to the punch list if you have one, and say so.

**D. Stop swallowing persistence errors.**

`GroceryApp/src/sync/sync-manager.ts` swallows a failed write into `console.warn` in two places, not one. The punch list cites only the first. Site one is the `persistListToDB(listId).catch(...)` inside the Yjs `update` observer (the `.catch` was near line 97 and the `console.warn` near 98). Site two is the identical construct on the remote-update path (near lines 190 and 191). Locate both by the surrounding code rather than by those numbers, and fix both; fixing one leaves the same failure invisible on the other.

"Surface it" here means user-visible, not telemetry. Sentry is dead in production (a separate goal owns `GroceryApp/src/services/sentry.ts`, where the DSN reads from an unset `process.env.SENTRY_DSN`) and `GroceryApp/src/error-handler.ts` is imported by nothing — confirm that with `grep -rn "error-handler" src/ App.tsx` — so routing errors there would route them nowhere. The working path already exists and is already used: `GroceryApp/src/sync/bootstrap.ts` does `useSyncStore.setState({ error: err.message })`, and `SyncIndicator` (`GroceryApp/src/components/SyncIndicator.tsx`) renders a red dot and a label whenever `syncState === 'error'`. Use that. Keep the promise rejection handled — an unhandled rejection inside a Yjs `update` observer is a worse outcome than a swallowed warning — and keep a console diagnostic alongside the user-visible signal. Note that `SyncIndicator`'s current "Sync error" label is not literally accurate for a local write failure; pick copy that does not mislead, and say what you chose and why. This is the exact failure class that silently loses data, which is why a signal the user can see beats a log line nobody reads.
</objective>

<constraints>
- **Never authenticate to, query, or otherwise exercise any live service using a credential found in this repository or its git history.** Two live read-write Turso tokens and the blind-RSA issuer private key are permanently in git history, and during an earlier pass an audit agent extracted one and ran queries against the production database. Do not repeat that, not to "verify it's live", not for testing. Rotation is a separate goal's work and rotation alone does not fix it: the token shipped as a client-side fallback (`settings.tursoToken || '<literal>'`), so any replacement shipped the same way is extractable from the APK.
- **Never write a recovery phrase, recovery seed, or key bytes into any file, screenshot, commit, or into `GOAL_PROMPT_NOTES.md`.** The repository is public and those twelve words *are* the master key — anything you record becomes world-readable. If you capture a screenshot of the new first-run screen, use a synthetic or redacted phrase and keep the image out of the repository: write it to a temporary directory outside the repo and tell the owner the exact path.
- **Do not discard working-tree state.** No `git stash`, `git checkout --`, `git reset --hard`, `git clean`, or branch switch in this working copy. These are irreversible, and if the persistence fix is uncommitted here it has no other copy — `git clean` in particular would delete an untracked `fresh-install-persistence.test.ts` outright. Commit to the launch branch you identified in `<setup>`, or leave the tree dirty. If you believe any of those commands is genuinely necessary, stop and get explicit owner confirmation first.
- **Do not force-push, rewrite history, or merge to the default branch.** Other goals own the branch and history work; a rewrite underneath them would strand this work.
- **Do not add new dependencies**, and specifically do not add `@testing-library/react-native` or `react-test-renderer` to write a component render test. The suite is pure-logic under ts-jest with no RN renderer — you can confirm that with `grep -n "react-test-renderer\|@testing-library" GroceryApp/package.json`, which should return nothing — and adding one is a native-adjacent dependency that changes the build and needs its own EAS validation. Use source-scanning tests in the `ac15-no-analytics.test.ts` idiom instead.
- **Do not delete or move the Trip Optimizer implementation or its tests.** Gate only. 1.x needs the code and the coverage.
- **Treat the persistence fix as someone else's unreviewed work, and expect other goals to be editing this working copy at the same time.** Item D touches `GroceryApp/src/sync/sync-manager.ts` and item C likely touches `GroceryApp/src/identity/recovery.ts`; neither is in the nine-file persistence set, so in the normal case you will not overlap at all. If you find yourself editing one that *is* in that set, say which and why — mixing your changes into an unreviewed P0 fix makes both hard to review.
- **Do not edit store-listing or marketing copy.** Another goal owns the store listings document. Report the conflict rather than fixing it, so two goals do not write the same file.
- **Keep `GOAL_PROMPT_NOTES.md` updated in place** at the repository root, one entry per decision, no duplicates. This is how the next session — which will not remember this one — learns what you decided and why. Add an entry when you make a call, not only at the end.
- **A green test against a mock is not evidence.** The suite passed while the app saved nothing, because `GroceryApp/__mocks__/watermelondb.ts` was more permissive than the real library. When a mock stands in for a real system, check the real semantics before trusting the result. A known live trap: that mock permits nested `database.write()` while real WatermelonDB deadlocks on it.
- **Do not stop early for token-budget or context-length reasons.** Context compacts automatically. Work the list to completion; if you must pause, leave `GOAL_PROMPT_NOTES.md` accurate enough that a fresh session resumes without re-deriving anything.
- If something in the source documents turns out to be wrong, say so plainly and show the evidence. Correcting the record is more useful than agreeing with it.
</constraints>

<done_when>
Each of these is proven by output you paste into the conversation, not by assertion. All commands run from `GroceryApp/` unless noted.

1. `grep -n "TRIP_OPTIMIZER_ENABLED" src/screens/GroceryListScreen.tsx` shows a module-level `const TRIP_OPTIMIZER_ENABLED = false;` with an explanatory doc comment. `grep -rn "<StopOptimizer\|<TripPlanSheet" src/` returns exactly two render sites: `<StopOptimizer` in `GroceryListScreen.tsx`, which you paste with surrounding lines proving it sits inside a `{TRIP_OPTIMIZER_ENABLED && ( … )}` guard, and `<TripPlanSheet` in `src/components/StopOptimizer.tsx`, which you state is unreachable transitively because `StopOptimizer` is its only parent. No guard is added inside `StopOptimizer.tsx`.
2. `grep -n "MANAGED_TIER_ENABLED\|VOICE_ASSISTANT_LINKING_ENABLED" src/screens/SettingsScreen.tsx` shows both consts still `false`, and you state in one sentence why the Managed Subscription block guarded by `{!isSelfHosted && (` cannot render while `MANAGED_TIER_ENABLED` is false.
3. A case-insensitive grep for `premium|upgrade|subscribe|subscription|paywall|unlock` across `src/` and `App.tsx` is pasted, and every hit is named and explained as a false positive or as deliberately-kept managed-tier plumbing. The "Managed Subscription" title, the "Subscription Key" label and the "Enter your subscription key" placeholder in `SettingsScreen.tsx` are explicitly addressed as live purchase strings that remain because their enclosing block cannot render.
4. A new Jest test in the `ac15-no-analytics.test.ts` source-scanning idiom guards the posture, and its output is pasted showing it green. It must prove its matcher actually discriminates by running that matcher against two inline fixture strings — one representing a correctly-guarded render site, one representing an unguarded one — and asserting the second is rejected, *before* applying the same matcher to the real file contents. Do not prove the negative case by flipping the flag to true or deleting the guard and re-running: that leaves a deliberate regression in a working tree that may hold an unreviewed P0 fix, and a session that dies mid-check leaves the flag on with nobody aware.
5. First-run recovery backup exists. Name the file and line where the flow is triggered, and the persisted acknowledgement flag with its default. Paste a `grep -rn` of the flag's name across `src/` and account for every hit — expect a type declaration in `src/types/index.ts`, a default in `src/config/settings.ts`, and one or more read sites — showing that exactly one hit *writes* the flag to `true`, inside the explicit "I've saved it" handler, so that dismissing or backgrounding provably cannot set it. There is no RN renderer in this repo and you may not add one, so this is a static proof, not a runtime one.
6. Paste `grep -n "persistListToDB\|console.warn" src/sync/sync-manager.ts`. Neither of the two `persistListToDB(...).catch(...)` sites — the one in the Yjs `update` observer and the one on the remote-update path — still terminates in a bare `console.warn`. Identify them by their surrounding code rather than by line number, since your edit shifts the numbers. Show the code path that reaches `useSyncStore` and a test covering it.
7. `npx tsc --noEmit` is pasted showing exit 0 and no output.
8. `npx jest` is pasted showing 0 failed, and suite and test counts no lower than the baseline you recorded in `<setup>`. Your new tests raise the count, so state your before-and-after numbers explicitly rather than claiming the totals are unchanged. As context only, the working copy this brief was written against reported 39 suites and 470 passed / 1 skipped before this goal's work.
9. `git status --short` and `git log --oneline -1` are pasted, showing that the nine persistence-fix files named in `<context>` are still present in the working copy — either still uncommitted, or committed on the launch branch, but in no case reverted or deleted — and that HEAD is still on the launch branch you identified in `<setup>`. Additional modified or untracked entries are expected, because other goals write in this working copy; their presence is not a failure and not a reason to stop.
10. `GOAL_PROMPT_NOTES.md` at the repository root contains a new dated section for this goal covering the gating decision, the first-run design choice, whether the `recoverFromPhrase` fix landed, and anything you deferred — with no phrase, seed, or key material in it.
</done_when>

<handoff>
Bring these to the owner rather than acting on them.

- **Store listing and marketing conflict, for whichever goal owns listings.** The drafted store assets advertise Trip Optimizer, which the v1 binary will not have — that conflict is the substance of this handoff and it holds whether or not you can open the file. In the working copy this brief was written against, the references sat in `audit-package/07-STORE-LISTINGS.md`: a listing bullet at line 68 ("Trip Optimizer: Costco + No Frills saves you $11.40 this week"), screenshot 4 of 8 at line 132 (the Trip Plan sheet), and the 18–24 second beat of the video storyboard at line 180. If that file is present in your working copy, confirm the three references around those lines and report the current line numbers; do not edit it. If it is absent, ask the owner where the listings live and hand the conflict over unedited either way, so the listings goal can rework the copy around what v1 actually is — the private, shared, offline-first family list.
- **Device proof of the first-run flow.** A simulator or emulator screenshot of the new recovery-backup step is the real proof it works, but it is not part of closing this goal. Find out whether it is possible here rather than assuming: `npx expo --version` tells you whether the Expo CLI resolves; on macOS, `xcrun simctl list devices available` lists iOS simulators and an empty or failing result means none is usable; `adb devices` lists Android emulators and devices, and an empty list or a missing binary means none is attached. If a target exists, attempt the build and capture the screenshot — redact or synthesize the phrase and keep the image outside the repository. If no target exists or the build fails, say so plainly, record the blocker in `GOAL_PROMPT_NOTES.md`, and hand the owner the exact command to run.
- **Anything requiring a login, an account, accepting terms or OAuth consent, a purchase, the Apple Team ID or signing certificates, generating the Android upload keystore, or pressing submit/publish.** Prepare everything up to that boundary, write out the exact values or commands to use, and batch the asks so the owner can clear several at once.
- **Any deletion of tracked files, history rewrite, or force-push** you believe is needed — these are irreversible and belong to other goals. Describe what and why; do not do it.
</handoff>

## 2 · `/goal` condition (3963 chars, limit 4000)

> This block is the acceptance checklist for the goal. If your agent session has a `/goal` command or an equivalent way to register a completion condition, pass this block to it after sending the prompt above; otherwise just keep it as the checklist you close against.

```
Goal 5 (PantryRun v1 product posture) is complete when the agent has surfaced all of the following in conversation as pasted command output, run from GroceryApp/ inside the repository root, with HEAD on the launch branch identified at session start: (1) `grep -n "TRIP_OPTIMIZER_ENABLED" src/screens/GroceryListScreen.tsx` showing a module-level `const TRIP_OPTIMIZER_ENABLED = false;` with a doc comment, plus `grep -rn "<StopOptimizer\|<TripPlanSheet" src/` returning exactly two render sites - `<StopOptimizer` in GroceryListScreen.tsx, with surrounding lines proving it sits inside a `{TRIP_OPTIMIZER_ENABLED && (...)}` guard, and `<TripPlanSheet` in src/components/StopOptimizer.tsx, unreachable transitively because StopOptimizer is its only parent; no guard may be added inside StopOptimizer.tsx, where that const is not in scope; (2) `grep -n "MANAGED_TIER_ENABLED\|VOICE_ASSISTANT_LINKING_ENABLED" src/screens/SettingsScreen.tsx` showing both consts still false, with one sentence on why the Managed Subscription block guarded by `{!isSelfHosted && (` cannot render while MANAGED_TIER_ENABLED is false; (3) a case-insensitive grep for premium|upgrade|subscribe|subscription|paywall|unlock over src/ and App.tsx in which every hit is named and explained as a false positive or as deliberately-kept managed-tier plumbing, including the "Managed Subscription" title, "Subscription Key" label and "Enter your subscription key" placeholder in SettingsScreen.tsx, literal purchase strings that stay because they sit inside that unreachable block; (4) a new Jest source-scanning test in the ac15-no-analytics.test.ts idiom, no new dependency, pasted green, proving its matcher discriminates by rejecting an inline unguarded-render-site fixture string and accepting a guarded one before applying that matcher to the real file - proving the negative by flipping the flag or deleting a guard is NOT acceptable, as it may leave a deliberate regression in a tree holding an unreviewed P0 fix; (5) a first-run recovery-phrase backup step, with the file and line that triggers it and the persisted acknowledgement flag with its default, plus a pasted `grep -rn` of the flag name across src/ in which every hit is accounted for (type declaration, default, reads) and exactly one hit writes the flag to true, inside the explicit acknowledgement handler, so dismissing or backgrounding provably cannot set it; (6) `grep -n "persistListToDB\|console.warn" src/sync/sync-manager.ts` showing that neither persistListToDB(...).catch(...) - the one in the Yjs update observer and the one on the remote-update path - still ends in a bare console.warn, identified by surrounding code since line numbers shift, plus the path reaching useSyncStore and a test covering it; (7) `npx tsc --noEmit` pasted with exit 0 and no output; (8) `npx jest` pasted with 0 failed and suite and test counts no lower than the pre-change baseline recorded at session start, with before-and-after numbers stated explicitly; (9) `git status --short` and `git log --oneline -1` pasted, showing the nine named persistence-fix files still present - uncommitted or already committed, but not reverted or deleted - and HEAD still on the launch branch; extra modified or untracked entries are expected, since other goals share this working copy, and are not a failure; (10) a new dated section in GOAL_PROMPT_NOTES.md at the repository root recording the gating decision, the first-run design, whether the recoverFromPhrase phrase-persistence fix landed, and any deferrals. Also confirmed in the final report: no repo or git-history credential used against any live service; no phrase, seed or key bytes written to any file, screenshot, commit or the notes; no git stash/checkout --/reset --hard/clean/branch switch/force-push/history rewrite; no new dependency; no Trip Optimizer code or test deleted; the store-listings document left unedited, its Trip Optimizer references reported to the owner instead.
```
