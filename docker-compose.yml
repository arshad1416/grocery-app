# GroceryApp Self-Host Stack
# docker compose up -d          # Start relay + optional price adapter
# docker compose down           # Stop everything
# docker compose build --pull   # Rebuild with fresh base images

services:
  # ──────────────────────────────────────────────
  # WebSocket Relay — Zero-knowledge Yjs sync
  # ──────────────────────────────────────────────
  relay:
    build:
      context: ./relay-server
      dockerfile: Dockerfile
      platforms:
        - linux/amd64
        - linux/arm64
    image: groceryapp-relay:latest
    container_name: groceryapp-relay
    restart: unless-stopped
    ports:
      - "8080:8080"    # WebSocket sync (auth via WS message) + REST (health, enroll, stats)
    environment:
      - NODE_ENV=production
      - RELAY_PORT=8080
      - RATE_LIMIT_MESSAGES=100
      - MAX_CLIENTS_PER_FAMILY=50
      - MAX_FAMILIES=100
      - MAX_DEVICES_PER_FAMILY=20
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    # No volumes — relay is stateless (all state on-device via WatermelonDB)
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: "256M"

networks:
  default:
    name: groceryapp-network
    driver: bridge
